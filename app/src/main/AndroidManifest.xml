<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/playstation"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WebStore"
        tools:targetApi="31">
        <activity
            android:name=".Seller.ViewInventory"
            android:exported="false"
            android:label="@string/title_activity_view_inventory"
            android:theme="@style/Theme.WebStore" />
        <activity
            android:name=".Seller.VendorDashboard"
            android:exported="false"
            android:label="@string/title_activity_vendor_dashboard"
            android:theme="@style/Theme.WebStore" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WebStore">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WebStore" />
    </application>

</manifest>